//I wasn't too sure how to exactly put this into code
//I am just going to comment it out and around it till i firgure it out.

/*
var symbal = X;

if (mouseClickedTotal === odd number) { //This is how player 1 and 2 will take turns.
  symbal === X;
}
else {
  symbal === O;
}

if (mouseClicked in region) { //Region of the board
  console.log(symbal);
*/
